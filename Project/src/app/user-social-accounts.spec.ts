import { UserSocialAccounts } from './user-social-accounts';

describe('UserSocialAccounts', () => {
  it('should create an instance', () => {
    expect(new UserSocialAccounts()).toBeTruthy();
  });
});
