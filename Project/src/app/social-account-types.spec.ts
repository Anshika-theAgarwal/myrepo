import { SocialAccountTypes } from './social-account-types';

describe('SocialAccountTypes', () => {
  it('should create an instance', () => {
    expect(new SocialAccountTypes()).toBeTruthy();
  });
});
